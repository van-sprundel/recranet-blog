{% extends 'base.html.twig' %}

{% block title %}Blogs{% endblock %}

{% block body %}
    <div class="">
        <h1 class="text-4xl text-center text-text my-2">
            Recent posts
        </h1>
        <div class="flex">
            <ul class="mx-auto">
                {% for blogPost in blog_posts %}
                    <li class="m-2 p-2">
                        {{ component('blog_post', { blogPost: blogPost }) }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>

{% endblock %}
